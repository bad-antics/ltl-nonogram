// examples/demo.ltl — runnable demo for ltl_nonogram

import std::io
use ltl_nonogram as lib

fn main() {
    io::println("ltl_nonogram demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}