// ltl-nonogram/main.ltl
//
// Nonogram / picross solver via constraint propagation.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-nonogram v0.1.0")
    io::println("Nonogram / picross solver via constraint propagation.")
    0
}
